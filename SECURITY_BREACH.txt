⚠️  SECURITY BREACH DETECTED ⚠️

The following secrets were committed to git:
1. JIRA_API_TOKEN (full token in config.fish.tmpl)
2. 1Password device ID (in op/config)

ACTION REQUIRED:
1. Revoke JIRA API token immediately at: https://id.atlassian.com/manage-profile/security/api-tokens
2. Generate new token
3. Store in 1Password
4. Rewrite git history to remove exposed secrets

